<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!-- These just start failing at random times, re-enable temporarily if you want to know -->
    <issue id="GradleDependency" severity="ignore"/>
    <issue id="NewerVersionAvailable" severity="ignore"/>
    <issue id="OldTargetApi" severity="ignore"/>

    <!-- Re-enable at i18n-time -->
    <issue id="HardcodedText" severity="ignore"/>
    <issue id="SetTextI18n" severity="ignore"/>

    <!-- Workaround for https://github.com/tony19/logback-android/issues/140 -->
    <issue id="InvalidPackage" severity="ignore"/>

    <!-- Maybe re-enable these and fix them properly -->
    <issue id="ClickableViewAccessibility" severity="ignore"/>
    <issue id="UselessParent" severity="ignore"/>
    <issue id="Registered" severity="ignore"/>
    <issue id="ExportedPreferenceActivity" severity="ignore"/>

    <!-- The main UI looks weird if we adhere to this -->
    <issue id="AlwaysShowAction" severity="ignore"/>
</lint>
